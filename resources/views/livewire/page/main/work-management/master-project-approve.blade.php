<div class="space-y-6">
    <div><a href="{{ route('work-management.master-projects.show', $masterProject) }}" wire:navigate class="text-sm text-blue-600">← Kembali</a><h1 class="mt-2 text-2xl font-semibold">Final Approval Master Project</h1><p class="text-sm text-gray-500">{{ $masterProject->name }}</p></div>
    <div class="rounded-xl border bg-white p-5"><h2 class="font-semibold">Division Projects</h2><div class="mt-4 divide-y">@foreach ($masterProject->divisionProjects as $project)<div class="flex items-center justify-between py-3 text-sm"><div><div class="font-medium">{{ $project->name }}</div><div class="text-gray-500">{{ $project->division?->name ?? '-' }} · {{ $project->manager?->user?->name ?? '-' }}</div></div><div>{{ str_replace('_', ' ', $project->status) }}</div></div>@endforeach</div></div>
    <form wire:submit="approve" class="space-y-3 rounded-xl border bg-white p-5"><select wire:model="decision" class="w-full rounded-lg border-gray-300"><option value="">Pilih keputusan</option><option value="approved">Approve</option><option value="rejected">Return untuk Revisi</option></select><textarea wire:model="feedback" rows="4" class="w-full rounded-lg border-gray-300" placeholder="Feedback final approval"></textarea><button class="rounded-lg bg-blue-600 px-4 py-2 text-sm text-white">Simpan Final Approval</button></form>
</div>
